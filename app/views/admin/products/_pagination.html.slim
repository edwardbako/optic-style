- if @products.size > 0
  = will_paginate @products, renderer: BootstrapPagination::Rails

  table.table.table-condensed
    thead
      tr
        th
        th Название
        th Артикул
        th Цена
        th Статус
        th
        th
    tbody = render @products

  = will_paginate @products, renderer: BootstrapPagination::Rails

- else
  ' Пока каталог пуст. Хотите добавить товары?