- if @product.present?
  - links = @product.parents.map {|pa| [pa.name, product_url(pa)]}.to_h
  = render 'layouts/header',
           links: {'Товары' => products_url}.merge(links).merge({current: @product.name})
- else
  = render 'layouts/header', links: {current: 'Товары'}

.container
  = render 'folders'
  br

  #products
    = render 'gallery'

  #load_more
    = render 'load_more'

  br
  br